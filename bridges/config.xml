<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    
  <data noupdate="1">

	<!-- groups (implied groups) on user Employee (default)  - we use pricelist because different currencies and no pricelist on the product -->
    <record id="base.group_user" model="res.groups" >
        <field name="implied_ids" eval="[(6,0, [
                                            ref('product.group_product_variant'),
                                            ref('stock.group_adv_location'),                                                                                      
                                                ])]" />  
    </record>
 
  </data>
    <record forcecreate="True" id="decimal_purchase_price_unit" model="decimal.precision">
        <field name="name">Purchase Price Unit</field>
        <field name="digits">2</field>
    </record>  
</odoo>
